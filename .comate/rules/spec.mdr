---
description:
globs:
alwaysApply: true
---
## Spec-Agent工作要求

以下规则仅针对Spec-Agent生效，如果你并非Spec-Agent则忽略此内容。

### Explore

在制定任务前，你应当彻底、完全、深入地探索代码库。

你可以更积极地使用SubAgent进行有目的性的代码查找，根据任务分出不同的需要探索和确认的内容，并由SubAgent取得结果和相应的文件。当使用SubAgent进行代码分析时，你应当尽量让SubAgent返回**有效**的文件路径，并且重新读取这些文件确认内容。

只有充分地理解代码库，确定与用户需求有关的信息后，才可以进入到后续的阶段。

### Doc

当生成`doc.md`文件时，注意以下要求：

- 不需要需求背景、用户故事、使用场景等信息，文档仅包含技术设计即可。
- 需要写明白哪些文件要被修改，修改的内容是什么，使用伪代码表达。
- 对于需要增加、修改的数据结构，需要包含在文档内。
- 文档要包含各个模块间的数据流动、出入参数，如组件的props、模块的导入导出。

### Tasks

当生成`tasks.md`时，遵循以下要求：

- 在更多的阶段进行验证工作，基础的任务使用`build`验证，阶段性成果需要通过`lint`和`type-check`验证。
- **不需要**在任务中出现需要用户参与的验证工作，如界面测试、性能测试均不需要。

### Coding

在实现编码阶段时，注意以下要求：

- 前序的文档、任务中，代码片段可能包含了一些简单的注释，用于理解和表达需求，但实际修改文件时应当移除这些注释，不要简单的照抄。
